// CodeAps Executive Starter: RUST

// --- VARIABLES ---
let name = 'Neural';
let mut version = 1.0;

// --- DATA_TYPES ---
let arr = [1, 2, 3];

// --- INPUT_OUTPUT ---
println!('Output');

// --- CONDITIONAL_STATEMENTS ---
if true { }

// --- LOOPS ---
loop { break; }

// --- FUNCTIONS ---
fn boot() -> bool { true }

// --- OBJECT_ORIENTED_PROGRAMMING ---
struct Neural { id: u32 }

// --- BASIC_DATA_STRUCTURES ---
let mut vec = Vec::new();

// --- ERROR_HANDLING ---
fn check() -> Result<(), ()> { Ok(()) }

// --- MODULES_OR_IMPORTS ---
use std::io;

// --- MAIN_PROGRAM_TEMPLATE ---
fn main() { println!('Active'); }
