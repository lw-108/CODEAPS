// CodeAps Executive Starter: JAVASCRIPT

// --- VARIABLES ---
let name = 'Neural';
const ver = 3.1;

// --- DATA_TYPES ---
let arr = [1,2,3];
let obj = { x: 1 };

// --- INPUT_OUTPUT ---
console.log('Output');

// --- CONDITIONAL_STATEMENTS ---
if (true) { console.log('OK'); }

// --- LOOPS ---
for (let i=0; i<5; i++) { }

// --- FUNCTIONS ---
const boot = () => true;

// --- OBJECT_ORIENTED_PROGRAMMING ---
class Neural { constructor() {} }

// --- BASIC_DATA_STRUCTURES ---
let map = new Map();

// --- ERROR_HANDLING ---
try { } catch (e) { }

// --- MODULES_OR_IMPORTS ---
import { x } from './mod';

// --- MAIN_PROGRAM_TEMPLATE ---
console.log('System Online');
